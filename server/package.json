{
  "name": "study-buddy-server",
  "version": "1.0.0",
  "private": true,
  "description": "Backend key proxy for StudyBuddy — holds the Claude API key server-side and proxies requests, so it never reaches the browser.",
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js"
  },
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "better-sqlite3": "^13.0.3",
    "cookie-parser": "^1.4.7",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  }
}
